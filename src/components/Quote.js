import React from 'react';

class Quote extends React.Component {

render(){
    return <blockquote className='quote'>"If opportunity doesn't knock, build a door."</blockquote>
    }
}

export default Quote;